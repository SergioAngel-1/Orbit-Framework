/**
 * @deprecated Este archivo es un vestigio de antes de la refactorización a módulos.
 * Usa directamente: import { authService } from './api'
 * o: import authApiService from './auth'
 */
export { authService as default } from './api';
